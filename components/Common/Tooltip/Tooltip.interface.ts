export interface TooltipProps {
  text: string;
  direction?: 'top' | 'bottom' | 'left' | 'right';
}

export interface TooltipBoxProps {
  text: string;
  direction?: 'top' | 'bottom' | 'left' | 'right';
}
